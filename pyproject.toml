[project]
name = "myrient-can-fixdat"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PyQt5>=5.15.10",
    "requests>=2.32.5",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
